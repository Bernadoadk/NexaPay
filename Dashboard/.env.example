# Console d'administration NexaPay.
#
# Prod recommandé : /api — le rewrite de vercel.json relaie vers l'API, donc
# pas de CORS à configurer.
# Alternative directe : https://nexapay-api.vercel.app/api
VITE_API_URL=/api
