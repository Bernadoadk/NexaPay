# Console d'administration NexaPay — projet Vercel distinct (Root Directory : Dashboard).
#
# Le rewrite de vercel.json relaie /api vers nexapay-api : aucun CORS à gérer,
# et l'URL de l'API n'est pas embarquée dans le bundle.
VITE_API_URL=/api
